<nav class="navbar bg-white dark:bg-gray-900 shadow-md transition-colors duration-300">
    <!-- Top Bar - Simplified -->
    <div class="hidden md:block bg-gray-50 dark:bg-gray-800 text-xs py-1.5 border-b border-gray-100 dark:border-gray-700">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center">
                <div class="flex items-center space-x-6">
                    <span class="flex items-center text-gray-600 dark:text-gray-400">
                        <i class="fas fa-shield-alt mr-1.5 text-blue-600 dark:text-blue-400"></i>
                        Certified Training
                    </span>
                    <span class="flex items-center text-gray-600 dark:text-gray-400">
                        <i class="fas fa-trophy mr-1.5 text-yellow-500"></i>
                        95% Pass Rate
                    </span>
                </div>
                <div class="flex items-center space-x-4">
                    <a href="/contact" class="text-gray-600 dark:text-gray-400 hover:text-blue-600 dark:hover:text-blue-400 transition-colors text-xs">
                        <i class="fas fa-headset mr-1"></i> Support
                    </a>
                    <span class="text-gray-300 dark:text-gray-600">|</span>
                    <a href="#" class="text-gray-600 dark:text-gray-400 hover:text-blue-600 dark:hover:text-blue-400 transition-colors text-xs">
                        <i class="fas fa-building mr-1"></i> Corporate
                    </a>
                </div>
            </div>
        </div>
    </div>

    <!-- Main Navigation -->
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
            
            <!-- Logo -->
            <div class="flex items-center space-x-2 cursor-pointer group" onclick="window.location.href='/'">
                <div class="w-10 h-10 bg-transparent rounded-md flex items-center justify-center">
                    <img src="/images/logo.png" alt="Logo" class="w-10 h-10 object-contain">
                </div>
                <div>
                    <div class="flex items-baseline">
                        <span class="text-xl font-bold bg-gradient-to-r from-blue-600 to-cyan-500 bg-clip-text text-transparent">Secure</span>
                        <span class="text-xl font-bold text-gray-800 dark:text-white">Aegix</span>
                    </div>
                    <div class="text-[10px] text-gray-500 dark:text-gray-400 tracking-wider">CYBERSECURITY</div>
                </div>
            </div>

            <!-- Desktop Navigation -->
            <div class="hidden md:flex items-center space-x-1">
                <a href="/" class="nav-link <%= currentPath === '/' ? 'active' : '' %>">
                    <i class="fas fa-home mr-1.5 text-sm"></i> Home
                </a>
                
                <!-- Courses Dropdown -->
                <div class="relative group">
                    <button class="nav-link flex items-center">
                        <i class="fas fa-graduation-cap mr-1.5 text-sm"></i> 
                        Courses
                        <i class="fas fa-chevron-down ml-1.5 text-xs transition-transform duration-200 group-hover:rotate-180"></i>
                    </button>
                    <div class="dropdown-menu max-h-[60vh] overflow-y-auto">
                        <div class="py-2">
                            <% if (navbarCourses && navbarCourses.length > 0) { %>
                                <% navbarCourses.forEach(course => { %>
                                    <a href="<%= course.url %>" class="dropdown-item text-blue-600 dark:text-blue-400 font-semibold">
                                        <i class="fas fa-graduation-cap w-4 mr-2 text-<%= course.color %>-600"></i> 
                                        <%= course.name %>
                                    </a>
                                <% }) %>
                                <hr class="my-2 border-gray-100 dark:border-gray-700">
                            <% } %>
                            <a href="/courses" class="dropdown-item text-blue-600 dark:text-blue-400 font-semibold">
                                <i class="fas fa-arrow-right mr-2"></i> View All Courses
                            </a>
                        </div>
                    </div>
                </div>

                <!-- Services Dropdown -->
                <div class="relative group">
                    <button class="nav-link flex items-center">
                        <i class="fas fa-cogs mr-1.5 text-sm"></i>
                        Services
                        <i class="fas fa-chevron-down ml-1.5 text-xs transition-transform duration-200 group-hover:rotate-180"></i>
                    </button>
                    <div class="dropdown-menu">
                        <div class="py-2">
                            <a href="/services/penetration-testing" class="dropdown-item">
                                <i class="fas fa-bug w-4 mr-2 text-blue-600"></i> Penetration Testing
                            </a>
                            <a href="/services/vulnerability-assessment" class="dropdown-item">
                                <i class="fas fa-search w-4 mr-2 text-cyan-600"></i> Vulnerability Assessment
                            </a>
                            <a href="/services/incident-response" class="dropdown-item">
                                <i class="fas fa-bell w-4 mr-2 text-red-600"></i> Incident Response
                            </a>
                            <a href="/services/red-team" class="dropdown-item">
                                <i class="fas fa-user-secret w-4 mr-2 text-purple-600"></i> Red Team Operations
                            </a>
                            <a href="/services/soc" class="dropdown-item">
                                <i class="fas fa-satellite-dish w-4 mr-2 text-teal-600"></i> Security Operations Center
                            </a>
                            <a href="/services/grc" class="dropdown-item">
                                <i class="fas fa-balance-scale w-4 mr-2 text-violet-600"></i> GRC Advisory
                            </a>
                        </div>
                    </div>
                </div>

                <a href="/about" class="nav-link <%= currentPath === '/about' ? 'active' : '' %>">
                    <i class="fas fa-info-circle mr-1.5 text-sm"></i> About
                </a>

                <a href="/blogs" class="nav-link">
                    <i class="fas fa-blog mr-1.5 text-sm"></i> Blog
                </a>
            </div>

            <!-- Right Actions -->
            <div class="flex items-center space-x-2">
                <!-- Search - Desktop with dropdown -->
                <div class="hidden lg:block relative">
                    <div class="relative">
                        <input type="text" id="searchInput" placeholder="Search courses, services, blogs..." 
                               class="w-64 px-3 py-1.5 pl-8 text-sm bg-gray-100 dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-md focus:outline-none focus:ring-1 focus:ring-blue-500 focus:border-blue-500 transition-all">
                        <i class="fas fa-search absolute left-2.5 top-2 text-gray-400 text-xs"></i>
                        <div id="searchResults" class="search-results hidden"></div>
                    </div>
                </div>

                <!-- Dark Mode Toggle -->
                <button id="darkModeToggle" class="theme-toggle">
                    <i id="darkModeIcon" class="fas fa-moon"></i>
                </button>

                <!-- User Section - Desktop View -->
                <% if (currUser) { %>
                    <div class="relative group hidden md:block">
                        <button class="user-menu-btn">
                            <div class="user-avatar">
                                <%= currUser.name.charAt(0).toUpperCase() %>
                            </div>
                            <i class="fas fa-chevron-down text-xs text-gray-500 transition-transform duration-200 group-hover:rotate-180"></i>
                        </button>
                        <div class="user-dropdown">
                            <div class="px-4 py-3 border-b border-gray-100 dark:border-gray-700">
                                <div class="font-semibold text-gray-900 dark:text-white"><%= currUser.name %></div>
                                <div class="text-xs text-gray-500 dark:text-gray-400"><%= currUser.email %></div>
                                <span class="inline-block mt-1 px-2 py-0.5 bg-blue-100 dark:bg-blue-900/30 text-blue-600 dark:text-blue-400 text-xs rounded-full capitalize">
                                    <%= currUser.role %>
                                </span>
                            </div>
                            <div class="py-2">
                                <% if (currUser.role === 'admin') { %>
                                    <a href="/admin/dashboard" class="user-dropdown-item">
                                        <i class="fas fa-tachometer-alt w-4"></i> Admin Dashboard
                                    </a>
                                    <a href="/admin/contacts" class="user-dropdown-item">
                                        <i class="fas fa-address-book w-4"></i> Contact Messages
                                    </a>
                                    <a href="/admin/subscribers" class="user-dropdown-item">
                                        <i class="fas fa-users w-4"></i> Subscribers
                                    </a>
                                    <a href="/admin/diploma" class="user-dropdown-item">
                                        <i class="fas fa-certificate w-4"></i> Diploma
                                    </a>

                                   
                                <% } else { %>
                                    <a href="/user/dashboard" class="user-dropdown-item">
                                        <i class="fas fa-chart-line w-4"></i> Dashboard
                                    </a>
                                <% } %>
                                <a href="/my-courses" class="user-dropdown-item">
                                    <i class="fas fa-book w-4"></i> My Courses
                                </a>
                                <a href="/profile" class="user-dropdown-item">
                                    <i class="fas fa-user w-4"></i> Profile
                                </a>
                                <hr class="my-1 border-gray-100 dark:border-gray-700">
                                <a href="/logout" class="user-dropdown-item text-red-600 dark:text-red-400">
                                    <i class="fas fa-sign-out-alt w-4"></i> Logout
                                </a>
                            </div>
                        </div>
                    </div>
                <% } else { %>
                    <div class="hidden md:flex items-center space-x-2">
                        <a href="/login" class="px-4 py-1.5 text-blue-600 dark:text-blue-400 text-sm font-medium hover:bg-blue-50 dark:hover:bg-gray-800 rounded-md transition-colors">
                            Login
                        </a>
                        <a href="/signup" class="px-4 py-1.5 bg-blue-600 hover:bg-blue-700 text-white text-sm font-medium rounded-md transition-colors">
                            Sign Up
                        </a>
                    </div>
                <% } %>

                <!-- Mobile Menu Button -->
                <button id="mobileMenuBtn" class="md:hidden w-8 h-8 rounded-md bg-gray-100 dark:bg-gray-800 text-gray-700 dark:text-gray-300">
                    <i class="fas fa-bars"></i>
                </button>
            </div>
        </div>
    </div>

    <!-- Mobile Menu -->
    <div id="mobileMenu" class="mobile-menu md:hidden">
        <div class="flex flex-col h-full">
            <!-- Mobile Menu Header -->
            <div class="flex justify-between items-center p-4 border-b border-gray-200 dark:border-gray-700">
                <div class="flex items-center space-x-2">
                    <div class="w-8 h-8 rounded-md flex items-center justify-center">
                        <img src="/images/logo.png" alt="Logo" class="w-8 h-8 object-contain">
                    </div>
                    <span class="font-bold text-lg text-gray-900 dark:text-white">SecureAegix</span>
                </div>
                <button id="closeMobileMenu" class="w-8 h-8 rounded-md bg-gray-100 dark:bg-gray-800 text-gray-700 dark:text-gray-300">
                    <i class="fas fa-times"></i>
                </button>
            </div>
            
            <div class="flex-1 overflow-y-auto py-4">
                <!-- Search in Mobile Menu -->
                <div class="px-4 mb-4">
                    <div class="relative">
                        <input type="text" id="mobileSearchInput" placeholder="Search courses, services, blogs..." 
                               class="w-full px-4 py-2 pl-10 bg-gray-100 dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-lg text-sm focus:outline-none focus:ring-2 focus:ring-blue-500">
                        <i class="fas fa-search absolute left-3 top-2.5 text-gray-400 text-sm"></i>
                    </div>
                    <div id="mobileSearchResults" class="mobile-search-results hidden"></div>
                </div>
                
                <!-- User Profile Section - Mobile Only -->
                <% if (currUser) { %>
                    <div class="px-4 pb-4 mb-4 border-b border-gray-200 dark:border-gray-700">
                        <div class="flex items-center gap-3 mb-3">
                            <div class="w-12 h-12 bg-gradient-to-r from-blue-600 to-cyan-500 rounded-full flex items-center justify-center text-white font-bold text-lg">
                                <%= currUser.name.charAt(0).toUpperCase() %>
                            </div>
                            <div>
                                <div class="font-semibold text-gray-900 dark:text-white"><%= currUser.name %></div>
                                <div class="text-xs text-gray-500 dark:text-gray-400"><%= currUser.email %></div>
                                <span class="inline-block mt-1 px-2 py-0.5 bg-blue-100 dark:bg-blue-900/30 text-blue-600 dark:text-blue-400 text-xs rounded-full capitalize">
                                    <%= currUser.role %>
                                </span>
                            </div>
                        </div>
                    </div>
                <% } %>
                
                <!-- Navigation Links -->
                <div class="space-y-1 px-4">
                    <a href="/" class="mobile-nav-link">
                        <i class="fas fa-home w-5 mr-3 text-blue-600"></i> Home
                    </a>
                    <a href="/courses" class="mobile-nav-link">
                        <i class="fas fa-graduation-cap w-5 mr-3 text-cyan-600"></i> Courses
                    </a>
                    <a href="/diploma" class="mobile-nav-link">
                        <i class="fas fa-certificate w-5 mr-3 text-yellow-600"></i> Diploma
                    </a>
                    <!-- Services Dropdown Mobile -->
                    <div class="mobile-dropdown">
                        <button class="mobile-dropdown-btn mobile-nav-link w-full justify-between">
                            <span class="flex items-center">
                                <i class="fas fa-cogs w-5 mr-3 text-gray-600 dark:text-gray-400"></i> 
                                Services
                            </span>
                            <i class="fas fa-chevron-down transition-transform duration-200"></i>
                        </button>
                        <div class="mobile-dropdown-menu hidden">
                            <a href="/services/penetration-testing" class="mobile-dropdown-item pl-12">
                                <i class="fas fa-bug w-4 mr-2 text-blue-600"></i> Penetration Testing
                            </a>
                            <a href="/services/vulnerability-assessment" class="mobile-dropdown-item pl-12">
                                <i class="fas fa-search w-4 mr-2 text-cyan-600"></i> Vulnerability Assessment
                            </a>
                            <a href="/services/incident-response" class="mobile-dropdown-item pl-12">
                                <i class="fas fa-bell w-4 mr-2 text-red-600"></i> Incident Response
                            </a>
                            <a href="/services/red-team" class="mobile-dropdown-item pl-12">
                                <i class="fas fa-user-secret w-4 mr-2 text-purple-600"></i> Red Team Operations
                            </a>
                            <a href="/services/soc" class="mobile-dropdown-item pl-12">
                                <i class="fas fa-satellite-dish w-4 mr-2 text-teal-600"></i> Security Operations Center
                            </a>
                            <a href="/services/grc" class="mobile-dropdown-item pl-12">
                                <i class="fas fa-balance-scale w-4 mr-2 text-violet-600"></i> GRC Advisory
                            </a>
                        </div>
                    </div>
                    
                    <a href="/about" class="mobile-nav-link">
                        <i class="fas fa-info-circle w-5 mr-3 text-green-600"></i> About
                    </a>
                    <a href="/blogs" class="mobile-nav-link">
                        <i class="fas fa-blog w-5 mr-3 text-purple-600"></i> Blog
                    </a>
                </div>
                
                <!-- User Actions Section - Mobile Only -->
                <% if (currUser) { %>
                    <div class="mt-6 pt-4 border-t border-gray-200 dark:border-gray-700">
                        <div class="space-y-1 px-4">
                            <div class="text-xs text-gray-500 uppercase tracking-wider px-4 mb-2">Account</div>
                            <% if (currUser.role === 'admin') { %>
                                <a href="/admin/dashboard" class="mobile-nav-link">
                                    <i class="fas fa-tachometer-alt w-5 mr-3 text-blue-600"></i> Admin Dashboard
                                </a>
                                <a href="/admin/contacts" class="mobile-nav-link">
                                    <i class="fas fa-address-book w-5 mr-3 text-blue-600"></i> Contact Messages
                                </a>
                                <a href="/admin/subscribers" class="mobile-nav-link">
                                    <i class="fas fa-users w-5 mr-3 text-blue-600"></i> Subscribers
                                </a>
                            <% } else { %>
                                <a href="/user/dashboard" class="mobile-nav-link">
                                    <i class="fas fa-chart-line w-5 mr-3 text-cyan-600"></i> My Dashboard
                                </a>
                            <% } %>
                            <a href="/my-courses" class="mobile-nav-link">
                                <i class="fas fa-book w-5 mr-3 text-green-600"></i> My Courses
                            </a>
                            <a href="/profile" class="mobile-nav-link">
                                <i class="fas fa-user w-5 mr-3 text-gray-600"></i> Profile Settings
                            </a>
                            <hr class="my-3 border-gray-200 dark:border-gray-700">
                            <a href="/logout" class="mobile-nav-link text-red-600 dark:text-red-400">
                                <i class="fas fa-sign-out-alt w-5 mr-3"></i> Logout
                            </a>
                        </div>
                    </div>
                <% } else { %>
                    <div class="mt-6 px-4 space-y-3">
                        <div class="text-xs text-gray-500 uppercase tracking-wider px-2 mb-2">Account Access</div>
                        <a href="/login" class="block text-center px-4 py-3 border border-blue-600 text-blue-600 rounded-lg font-medium hover:bg-blue-50 transition-all">
                            <i class="fas fa-sign-in-alt mr-2"></i> Login
                        </a>
                        <a href="/signup" class="block text-center px-4 py-3 bg-gradient-to-r from-blue-600 to-cyan-600 text-white rounded-lg font-medium hover:shadow-lg transition-all">
                            <i class="fas fa-user-plus mr-2"></i> Sign Up
                        </a>
                    </div>
                <% } %>
            </div>
        </div>
    </div>
</nav>

<style>
/* Existing styles remain the same... */
.navbar {
    position: sticky;
    top: 0;
    z-index: 1000;
}

.nav-link {
    display: flex;
    align-items: center;
    padding: 0.5rem 1rem;
    font-size: 0.875rem;
    font-weight: 500;
    color: #4b5563;
    border-radius: 0.375rem;
    transition: all 0.2s;
}

.dark .nav-link {
    color: #d1d5db;
}

.nav-link:hover {
    background-color: #f3f4f6;
    color: #2563eb;
}

.dark .nav-link:hover {
    background-color: #1f2937;
    color: #60a5fa;
}

.nav-link.active {
    background-color: #eff6ff;
    color: #2563eb;
}

.dark .nav-link.active {
    background-color: #1e3a8a;
    color: #60a5fa;
}

/* Dropdown Menu */
.dropdown-menu {
    position: absolute;
    top: 100%;
    left: 0;
    margin-top: 0.5rem;
    width: 240px;
    background-color: white;
    border-radius: 0.5rem;
    box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
    opacity: 0;
    visibility: hidden;
    transform: translateY(-10px);
    transition: all 0.2s;
    z-index: 50;
    border: 1px solid #e5e7eb;
}

.dark .dropdown-menu {
    background-color: #1f2937;
    border-color: #374151;
}

.group:hover .dropdown-menu {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
}

.dropdown-item {
    display: flex;
    align-items: center;
    padding: 0.5rem 1rem;
    font-size: 0.875rem;
    color: #4b5563;
    transition: all 0.2s;
}

.dark .dropdown-item {
    color: #d1d5db;
}

.dropdown-item:hover {
    background-color: #f3f4f6;
    color: #2563eb;
}

.dark .dropdown-item:hover {
    background-color: #374151;
    color: #60a5fa;
}

/* Theme Toggle Button */
.theme-toggle {
    width: 2rem;
    height: 2rem;
    border-radius: 0.375rem;
    background-color: #f3f4f6;
    color: #4b5563;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.2s;
}

.dark .theme-toggle {
    background-color: #1f2937;
    color: #d1d5db;
}

.theme-toggle:hover {
    background-color: #e5e7eb;
}

.dark .theme-toggle:hover {
    background-color: #374151;
}

/* User Menu */
.user-menu-btn {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.25rem 0.5rem;
    border-radius: 0.5rem;
    background: linear-gradient(135deg, #eff6ff 0%, #ecfeff 100%);
    transition: all 0.2s;
}

.dark .user-menu-btn {
    background: #1f2937;
}

.user-avatar {
    width: 1.75rem;
    height: 1.75rem;
    background: linear-gradient(135deg, #3b82f6, #06b6d4);
    border-radius: 9999px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-size: 0.75rem;
    font-weight: 600;
}

.user-dropdown {
    position: absolute;
    right: 0;
    top: 100%;
    margin-top: 0.5rem;
    width: 260px;
    background-color: white;
    border-radius: 0.5rem;
    box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
    opacity: 0;
    visibility: hidden;
    transform: translateY(-10px);
    transition: all 0.2s;
    z-index: 50;
    border: 1px solid #e5e7eb;
}

.dark .user-dropdown {
    background-color: #1f2937;
    border-color: #374151;
}

.group:hover .user-dropdown {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
}

.user-dropdown-item {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.5rem 1rem;
    font-size: 0.875rem;
    color: #4b5563;
    transition: all 0.2s;
}

.dark .user-dropdown-item {
    color: #d1d5db;
}

.user-dropdown-item:hover {
    background-color: #f3f4f6;
}

.dark .user-dropdown-item:hover {
    background-color: #374151;
}

/* ============================================ */
/* SEARCH RESULTS STYLES */
/* ============================================ */

.search-results {
    position: absolute;
    top: 100%;
    left: 0;
    right: 0;
    margin-top: 0.5rem;
    background-color: white;
    border-radius: 0.75rem;
    box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
    max-height: 60vh;
    overflow-y: auto;
    z-index: 1000;
    border: 1px solid #e5e7eb;
}

.dark .search-results {
    background-color: #1f2937;
    border-color: #374151;
}

.search-result-item {
    display: flex;
    padding: 0.75rem 1rem;
    transition: all 0.2s;
    cursor: pointer;
    border-bottom: 1px solid #f3f4f6;
}

.dark .search-result-item {
    border-bottom-color: #374151;
}

.search-result-item:hover {
    background-color: #f9fafb;
}

.dark .search-result-item:hover {
    background-color: #374151;
}

.search-result-image {
    width: 48px;
    height: 48px;
    border-radius: 0.5rem;
    object-fit: cover;
    flex-shrink: 0;
    background-color: #f3f4f6;
}

.search-result-content {
    flex: 1;
    margin-left: 0.75rem;
}

.search-result-title {
    font-weight: 600;
    font-size: 0.875rem;
    color: #111827;
    margin-bottom: 0.25rem;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    flex-wrap: wrap;
}

.dark .search-result-title {
    color: #f9fafb;
}

.search-result-badge {
    font-size: 0.625rem;
    padding: 0.125rem 0.375rem;
    border-radius: 9999px;
    font-weight: 500;
}

.badge-course { background-color: #dbeafe; color: #1e40af; }
.badge-blog { background-color: #f3e8ff; color: #6b21a8; }
.badge-page { background-color: #dcfce7; color: #166534; }
.badge-service { background-color: #fed7aa; color: #92400e; }

.dark .badge-course { background-color: #1e3a8a; color: #93c5fd; }
.dark .badge-blog { background-color: #4c1d95; color: #d8b4fe; }
.dark .badge-page { background-color: #064e3b; color: #6ee7b7; }
.dark .badge-service { background-color: #78350f; color: #fcd34d; }

.search-result-description {
    font-size: 0.75rem;
    color: #6b7280;
    margin-top: 0.125rem;
    line-height: 1.4;
}

.dark .search-result-description {
    color: #9ca3af;
}

.search-result-price {
    font-size: 0.75rem;
    font-weight: 600;
    color: #059669;
    margin-top: 0.125rem;
}

.search-result-icon {
    width: 40px;
    height: 40px;
    border-radius: 0.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.25rem;
    flex-shrink: 0;
}

.search-empty {
    padding: 2rem;
    text-align: center;
    color: #6b7280;
    font-size: 0.875rem;
}

.dark .search-empty {
    color: #9ca3af;
}

.search-loading {
    padding: 1rem;
    text-align: center;
    color: #6b7280;
    font-size: 0.75rem;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
}

/* Mobile Search Results */
.mobile-search-results {
    margin-top: 0.5rem;
    background-color: white;
    border-radius: 0.75rem;
    box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
    max-height: 400px;
    overflow-y: auto;
    border: 1px solid #e5e7eb;
}

.dark .mobile-search-results {
    background-color: #1f2937;
    border-color: #374151;
}

/* Scrollbar Styling */
.search-results::-webkit-scrollbar,
.mobile-search-results::-webkit-scrollbar {
    width: 6px;
}

.search-results::-webkit-scrollbar-track,
.mobile-search-results::-webkit-scrollbar-track {
    background: #f1f1f1;
    border-radius: 3px;
}

.search-results::-webkit-scrollbar-thumb,
.mobile-search-results::-webkit-scrollbar-thumb {
    background: #cbd5e1;
    border-radius: 3px;
}

.search-results::-webkit-scrollbar-thumb:hover,
.mobile-search-results::-webkit-scrollbar-thumb:hover {
    background: #94a3b8;
}

/* Mobile Menu Styles (keep your existing mobile menu styles) */
.mobile-menu {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-color: white;
    z-index: 9999;
    transform: translateX(-100%);
    transition: transform 0.3s ease-in-out;
    display: block !important;
}

.dark .mobile-menu {
    background-color: #111827;
}

.mobile-menu.open {
    transform: translateX(0);
}

.mobile-nav-link {
    display: flex;
    align-items: center;
    padding: 0.75rem 1rem;
    border-radius: 0.5rem;
    font-weight: 500;
    font-size: 1rem;
    color: #4b5563;
    transition: all 0.2s;
}

.dark .mobile-nav-link {
    color: #d1d5db;
}

.mobile-nav-link:hover {
    background-color: #f3f4f6;
}

.dark .mobile-nav-link:hover {
    background-color: #1f2937;
}

.mobile-dropdown-btn {
    width: 100%;
    justify-content: space-between;
    background: none;
    cursor: pointer;
}

.mobile-dropdown-menu {
    display: none;
}

.mobile-dropdown-menu.show {
    display: block;
}

.mobile-dropdown-item {
    display: flex;
    align-items: center;
    padding: 0.75rem 1rem 0.75rem 3rem;
    border-radius: 0.5rem;
    font-weight: 500;
    font-size: 0.875rem;
    color: #4b5563;
}

.dark .mobile-dropdown-item {
    color: #d1d5db;
}

/* Animation */
@keyframes slideIn {
    from {
        opacity: 0;
        transform: translateX(-20px);
    }
    to {
        opacity: 1;
        transform: translateX(0);
    }
}

.mobile-menu.open .mobile-nav-link {
    animation: slideIn 0.3s ease forwards;
    opacity: 0;
}
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
    
    // ============================================
    // SEARCH FUNCTIONALITY WITH DEBOUNCING
    // ============================================
    
    const searchInput = document.getElementById('searchInput');
    const mobileSearchInput = document.getElementById('mobileSearchInput');
    const searchResults = document.getElementById('searchResults');
    const mobileSearchResults = document.getElementById('mobileSearchResults');
    
    let debounceTimer;
    let currentSearchQuery = '';
    
    // Debounce function
    function debounce(func, delay) {
        return function(...args) {
            clearTimeout(debounceTimer);
            debounceTimer = setTimeout(() => func.apply(this, args), delay);
        };
    }
    
    // Function to perform search
    async function performSearch(query, isMobile = false) {
        if (!query || query.length < 2) {
            if (isMobile && mobileSearchResults) {
                mobileSearchResults.classList.add('hidden');
                mobileSearchResults.innerHTML = '';
            } else if (searchResults) {
                searchResults.classList.add('hidden');
                searchResults.innerHTML = '';
            }
            return;
        }
        
        currentSearchQuery = query;
        
        const resultsContainer = isMobile ? mobileSearchResults : searchResults;
        if (!resultsContainer) return;
        
        // Show loading state
        resultsContainer.innerHTML = `
            <div class="search-loading">
                <i class="fas fa-spinner fa-spin"></i>
                <span>Searching...</span>
            </div>
        `;
        resultsContainer.classList.remove('hidden');
        
        try {
            const response = await fetch(`/api/search?q=${encodeURIComponent(query)}`);
            const data = await response.json();
            
            if (data.results && data.results.length > 0) {
                displaySearchResults(data.results, resultsContainer, isMobile);
            } else {
                resultsContainer.innerHTML = `
                    <div class="search-empty">
                        <i class="fas fa-search text-gray-400 mb-2 block text-2xl"></i>
                        <p>No results found for "${escapeHtml(query)}"</p>
                        <p class="text-xs mt-1">Try searching for courses, services, or pages</p>
                    </div>
                `;
            }
        } catch (error) {
            console.error('Search error:', error);
            resultsContainer.innerHTML = `
                <div class="search-empty">
                    <i class="fas fa-exclamation-triangle text-red-400 mb-2 block text-2xl"></i>
                    <p>Search failed. Please try again.</p>
                </div>
            `;
        }
    }
    
    // Function to display search results
    function displaySearchResults(results, container, isMobile) {
        const getColorClass = (color) => {
            const colors = {
                blue: 'text-blue-600',
                cyan: 'text-cyan-600',
                green: 'text-green-600',
                purple: 'text-purple-600',
                red: 'text-red-600',
                indigo: 'text-indigo-600',
                gray: 'text-gray-600'
            };
            return colors[color] || 'text-blue-600';
        };
        
        const getBadgeClass = (type) => {
            switch(type) {
                case 'course': return 'badge-course';
                case 'blog': return 'badge-blog';
                case 'service': return 'badge-service';
                default: return 'badge-page';
            }
        };
        
        const getBadgeText = (type) => {
            switch(type) {
                case 'course': return 'COURSE';
                case 'blog': return 'BLOG';
                case 'service': return 'SERVICE';
                default: return 'PAGE';
            }
        };
        
        const html = results.map(result => `
            <div class="search-result-item" onclick="window.location.href='${result.url}'">
                ${result.image ? `
                    <img src="${result.image}" alt="${escapeHtml(result.title)}" class="search-result-image">
                ` : `
                    <div class="search-result-icon bg-gray-100 dark:bg-gray-800 ${getColorClass(result.color)}">
                        <i class="${result.icon}"></i>
                    </div>
                `}
                <div class="search-result-content">
                    <div class="search-result-title">
                        ${escapeHtml(result.title)}
                        <span class="search-result-badge ${getBadgeClass(result.type)}">${getBadgeText(result.type)}</span>
                    </div>
                    ${result.description ? `<div class="search-result-description">${escapeHtml(result.description.substring(0, 100))}${result.description.length > 100 ? '...' : ''}</div>` : ''}
                    ${result.price ? `<div class="search-result-price">₹${result.price}</div>` : ''}
                </div>
            </div>
        `).join('');
        
        container.innerHTML = html;
        container.classList.remove('hidden');
    }
    
    // Helper function to escape HTML
    function escapeHtml(text) {
        if (!text) return '';
        const div = document.createElement('div');
        div.textContent = text;
        return div.innerHTML;
    }
    
    // Close search results when clicking outside
    function closeSearchOnClickOutside(event, container, inputElement) {
        if (container && !container.contains(event.target) && event.target !== inputElement) {
            container.classList.add('hidden');
        }
    }
    
    // Desktop search handler with debouncing
    if (searchInput && searchResults) {
        const debouncedSearch = debounce((query) => performSearch(query, false), 300);
        
        searchInput.addEventListener('input', (e) => {
            const query = e.target.value.trim();
            debouncedSearch(query);
        });
        
        searchInput.addEventListener('focus', () => {
            if (currentSearchQuery && currentSearchQuery.length >= 2 && searchResults.innerHTML && !searchResults.classList.contains('hidden')) {
                // Keep visible if already has results
            } else if (currentSearchQuery && currentSearchQuery.length >= 2) {
                performSearch(currentSearchQuery, false);
            }
        });
        
        // Close search results when clicking outside
        document.addEventListener('click', (e) => {
            if (searchResults && !searchResults.contains(e.target) && e.target !== searchInput) {
                searchResults.classList.add('hidden');
            }
        });
    }
    
    // Mobile search handler
    if (mobileSearchInput && mobileSearchResults) {
        const debouncedMobileSearch = debounce((query) => performSearch(query, true), 300);
        
        mobileSearchInput.addEventListener('input', (e) => {
            const query = e.target.value.trim();
            debouncedMobileSearch(query);
        });
        
        // Close mobile search results when clicking outside
        document.addEventListener('click', (e) => {
            if (mobileSearchResults && !mobileSearchResults.contains(e.target) && e.target !== mobileSearchInput) {
                mobileSearchResults.classList.add('hidden');
            }
        });
    }
    
    // ============================================
    // MOBILE MENU TOGGLE
    // ============================================
    
    const mobileMenuBtn = document.getElementById('mobileMenuBtn');
    const mobileMenu = document.getElementById('mobileMenu');
    const closeMobileMenu = document.getElementById('closeMobileMenu');
    
    if (mobileMenuBtn && mobileMenu) {
        function openMobileMenu() {
            const scrollY = window.scrollY;
            mobileMenu.classList.add('open');
            document.body.style.position = 'fixed';
            document.body.style.top = `-${scrollY}px`;
            document.body.style.width = '100%';
            document.body.style.overflow = 'hidden';
            document.body.dataset.scrollY = scrollY;
        }
        
        function closeMobileMenuFunc() {
            const scrollY = document.body.dataset.scrollY;
            mobileMenu.classList.remove('open');
            document.body.style.position = '';
            document.body.style.top = '';
            document.body.style.width = '';
            document.body.style.overflow = '';
            if (scrollY) {
                window.scrollTo(0, parseInt(scrollY));
            }
            delete document.body.dataset.scrollY;
        }
        
        mobileMenuBtn.addEventListener('click', (e) => {
            e.preventDefault();
            e.stopPropagation();
            openMobileMenu();
        });
        
        if (closeMobileMenu) {
            closeMobileMenu.addEventListener('click', (e) => {
                e.preventDefault();
                closeMobileMenuFunc();
            });
        }
        
        mobileMenu.addEventListener('click', (e) => {
            if (e.target === mobileMenu) {
                closeMobileMenuFunc();
            }
        });
        
        document.addEventListener('keydown', (e) => {
            if (e.key === 'Escape' && mobileMenu.classList.contains('open')) {
                closeMobileMenuFunc();
            }
        });
    }
    
    // ============================================
    // MOBILE SERVICES DROPDOWN
    // ============================================
    
    const mobileDropdownBtns = document.querySelectorAll('.mobile-dropdown-btn');
    
    mobileDropdownBtns.forEach(btn => {
        btn.addEventListener('click', function(e) {
            e.preventDefault();
            e.stopPropagation();
            
            const parent = this.closest('.mobile-dropdown');
            const dropdownMenu = parent.querySelector('.mobile-dropdown-menu');
            const icon = this.querySelector('i:last-child');
            
            mobileDropdownBtns.forEach(otherBtn => {
                if (otherBtn !== btn) {
                    const otherParent = otherBtn.closest('.mobile-dropdown');
                    const otherMenu = otherParent.querySelector('.mobile-dropdown-menu');
                    const otherIcon = otherBtn.querySelector('i:last-child');
                    if (otherMenu) {
                        otherMenu.classList.remove('show');
                        if (otherIcon) otherIcon.style.transform = 'rotate(0deg)';
                    }
                }
            });
            
            if (dropdownMenu) {
                dropdownMenu.classList.toggle('show');
                if (dropdownMenu.classList.contains('show')) {
                    if (icon) icon.style.transform = 'rotate(180deg)';
                } else {
                    if (icon) icon.style.transform = 'rotate(0deg)';
                }
            }
        });
    });
    
    // ============================================
    // DARK MODE TOGGLE
    // ============================================
    
    const darkModeToggle = document.getElementById('darkModeToggle');
    const darkModeIcon = document.getElementById('darkModeIcon');
    
    if (darkModeToggle) {
        const savedMode = localStorage.getItem('darkMode');
        if (savedMode === 'true') {
            document.documentElement.classList.add('dark');
            if (darkModeIcon) {
                darkModeIcon.classList.remove('fa-moon');
                darkModeIcon.classList.add('fa-sun');
            }
        } else if (savedMode === 'false') {
            document.documentElement.classList.remove('dark');
            if (darkModeIcon) {
                darkModeIcon.classList.remove('fa-sun');
                darkModeIcon.classList.add('fa-moon');
            }
        } else {
            if (window.matchMedia && window.matchMedia('(prefers-color-scheme: dark)').matches) {
                document.documentElement.classList.add('dark');
                if (darkModeIcon) {
                    darkModeIcon.classList.remove('fa-moon');
                    darkModeIcon.classList.add('fa-sun');
                }
            }
        }
        
        darkModeToggle.addEventListener('click', function() {
            const isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('darkMode', isDark);
            
            if (darkModeIcon) {
                if (isDark) {
                    darkModeIcon.classList.remove('fa-moon');
                    darkModeIcon.classList.add('fa-sun');
                } else {
                    darkModeIcon.classList.remove('fa-sun');
                    darkModeIcon.classList.add('fa-moon');
                }
            }
            
            fetch('/toggle-darkmode', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ darkMode: isDark })
            }).catch(err => console.error('Dark mode sync error:', err));
        });
    }
    
    // ============================================
    // NAVBAR SCROLL EFFECT
    // ============================================
    
    const navbar = document.querySelector('.navbar');
    if (navbar) {
        window.addEventListener('scroll', function() {
            if (window.scrollY > 50) {
                navbar.classList.add('shadow-xl');
                navbar.classList.add('bg-opacity-95');
                navbar.classList.add('backdrop-blur-sm');
            } else {
                navbar.classList.remove('shadow-xl');
                navbar.classList.remove('bg-opacity-95');
                navbar.classList.remove('backdrop-blur-sm');
            }
        });
    }
    
    // ============================================
    // ACTIVE LINK HIGHLIGHTING
    // ============================================
    
    const currentPath = window.location.pathname;
    const navLinks = document.querySelectorAll('.nav-link');
    
    navLinks.forEach(link => {
        const href = link.getAttribute('href');
        if (href && href !== '#') {
            if (href === currentPath || (href === '/' && currentPath === '/')) {
                link.classList.add('active');
            } else {
                link.classList.remove('active');
            }
        }
    });
});

// Handle window resize
window.addEventListener('resize', function() {
    const mobileMenu = document.getElementById('mobileMenu');
    const isDesktop = window.innerWidth >= 768;
    
    if (isDesktop && mobileMenu && mobileMenu.classList.contains('open')) {
        mobileMenu.classList.remove('open');
        document.body.style.position = '';
        document.body.style.top = '';
        document.body.style.width = '';
        document.body.style.overflow = '';
    }
});
</script>